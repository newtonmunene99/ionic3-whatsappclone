import { SerializeurlPipe } from './serializeurl.pipe';

describe('SerializeurlPipe', () => {
  it('create an instance', () => {
    const pipe = new SerializeurlPipe();
    expect(pipe).toBeTruthy();
  });
});
